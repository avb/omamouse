#!/bin/bash
# Install lan-mouse. Pairing happens in the bar panel over Tailscale.
set -euo pipefail
omarchy pkg add lan-mouse wl-clipboard
echo "Packages installed. Click the icon, turn it on, and pick a Tailscale machine."
